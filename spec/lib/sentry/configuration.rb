describe Sentry::Configuration do

  # check the defaults
  # check the singleton

  describe "a new configuration" do
    
    before :each do
      @config = Sentry::Configuration.new
    end
    
  end
  
end